class Phase {
  int id;
  String name;
  String description;
  int scoreActivities;

  Phase({
    required this.id,
    required this.name,
    required this.description,
    required this.scoreActivities,
  });
}
